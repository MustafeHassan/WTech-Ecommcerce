import 'package:flutter/material.dart';

class StatisticsPageTablet extends StatelessWidget {
  const StatisticsPageTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Statistics Tablet'),
    );
  }
}
