import 'package:flutter/material.dart';

class StatisticsPageDesktop extends StatelessWidget {
  const StatisticsPageDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Statistics Desktop'),
    );
  }
}
