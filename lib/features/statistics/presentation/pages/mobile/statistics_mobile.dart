import 'package:flutter/material.dart';

class StatisticsPageMobile extends StatelessWidget {
  const StatisticsPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Statistics Mobile'),
    );
  }
}
